{
  "name": "yieldstar-packages",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' build --watch",
    "start": "bun scripts/run-example.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer": "^9.0.7",
    "inquirer": "^9.2.23"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
