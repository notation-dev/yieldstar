packages:
  - packages/*
  - examples
  - test
  - docs

allowBuilds:
  esbuild: true
  lightningcss: true
  # Pulled in by Wrangler's image bindings, which the docs site does not use.
  sharp: false
  workerd: true

overrides:
  shell-quote@<1.8.4: "^1.8.4"
  tmp@<0.2.6: "^0.2.6"
